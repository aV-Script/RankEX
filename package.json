{
  "name": "rank ex",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:analyze": "ANALYZE=true vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run --passWithNoTests",
    "deploy:rules":      "cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only firestore:rules --project fitquest-60a09",
    "deploy:rules:dev":  "cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only firestore:rules --project rankex-dev",
    "deploy:indexes": "cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only firestore:indexes --project fitquest-60a09",
    "deploy:app":       "npm run build && cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only hosting:app --project fitquest-60a09",
    "deploy:admin":     "npm run build && cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only hosting:admin --project fitquest-60a09",
    "deploy:all":       "npm run build && cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only hosting --project fitquest-60a09",
    "deploy:app:dev":   "cross-env NODE_OPTIONS=--dns-result-order=ipv4first vite build --mode development && cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only hosting:app --project rankex-dev",
    "deploy:admin:dev": "cross-env NODE_OPTIONS=--dns-result-order=ipv4first vite build --mode development && cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only hosting:admin --project rankex-dev",
    "deploy:all:dev":   "cross-env NODE_OPTIONS=--dns-result-order=ipv4first vite build --mode development && cross-env NODE_OPTIONS=--dns-result-order=ipv4first firebase deploy --only hosting --project rankex-dev",
    "migrate:dataNascita":      "node scripts/migrate-dataNascita.mjs",
    "migrate:dataNascita:prod": "node scripts/migrate-dataNascita.mjs --prod"
  },
  "dependencies": {
    "firebase": "^10.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.13.1",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-react": "^4.3.1",
    "cross-env": "^10.1.0",
    "eslint": "^9.39.4",
    "eslint-plugin-import": "^2.32.0",
    "rollup-plugin-visualizer": "^5.12.0",
    "tailwindcss": "^4.0.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
